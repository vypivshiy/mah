# Autogenerated. DO NOT EDIT

from __future__ import annotations

from dataclasses import dataclass, field
from typing import ClassVar, Any, TypedDict
import models as m


@dataclass
class PingPayload:
    interactive: bool

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "interactive": "interactive",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

@dataclass
class LogParameters:
    events: list[m.LogLogEvent] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "events": "events",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

EmptyResponse = TypedDict("EmptyResponse", {}, total=False)

@dataclass
class SessionInitPayload:
    userAgent: m.UserAgent
    deviceId: str
    clientSessionId: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "userAgent": "userAgent",
        "deviceId": "deviceId",
        "clientSessionId": "clientSessionId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

SessionInitResponse = TypedDict(
    "SessionInitResponse",
    {
    "app-update-type": int | None,
    "reg-country-code": list[str] | None,
    "lang": bool | None,
    },
    total=False,
)

@dataclass
class ProfileParameters:
    firstName: str | None = None
    lastName: str | None = None
    crop: m.Crop | None = None
    photoToken: str | None = None
    photoId: int | None = None
    link: str | None = None
    description: str | None = None
    avatarType: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "firstName": "firstName",
        "lastName": "lastName",
        "crop": "crop",
        "photoToken": "photoToken",
        "photoId": "photoId",
        "link": "link",
        "description": "description",
        "avatarType": "avatarType",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ProfileResponse = TypedDict(
    "ProfileResponse",
    {
    "profile": m.Profile,
    },
    total=False,
)

@dataclass
class AuthOneMeAuthRequestParameters:
    phone: str
    type: str
    language: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "phone": "phone",
        "language": "language",
        "type": "type",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthOneMeAuthRequestResponse = TypedDict(
    "AuthOneMeAuthRequestResponse",
    {
    "token": str,
    "codeLength": int,
    "altActionDuration": int,
    "requestMaxDuration": int | None,
    "requestCountLeft": int | None,
    },
    total=False,
)

@dataclass
class AuthOneMeAuthParameters:
    token: str
    verifyCode: str
    authTokenType: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "token": "token",
        "verifyCode": "verifyCode",
        "authTokenType": "authTokenType",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthOneMeAuthResponse = TypedDict(
    "AuthOneMeAuthResponse",
    {
    "tokenAttrs": dict[str, m.AuthOneMeTokenAttributes],
    "presetAvatars": list[m.PresetAvatarsInfo] | None,
    "profile": m.Profile | None,
    "passwordChallenge": m.AuthOneMePasswordChallenge | None,
    },
    total=False,
)

@dataclass
class AuthLoginParameters:
    token: str
    chatsSync: int
    contactsSync: int
    presenceSync: int
    callsSync: int
    draftsSync: int
    configHash: str
    interactive: bool
    lastLogin: int
    bannersSync: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "token": "token",
        "chatsSync": "chatsSync",
        "contactsSync": "contactsSync",
        "presenceSync": "presenceSync",
        "callsSync": "callsSync",
        "draftsSync": "draftsSync",
        "bannersSync": "bannersSync",
        "configHash": "configHash",
        "interactive": "interactive",
        "lastLogin": "lastLogin",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthLoginResponse = TypedDict(
    "AuthLoginResponse",
    {
    "time": int,
    "profile": m.Profile,
    "chats": list[m.Chat],
    "contacts": list[m.Contact],
    "chatMarker": int | None,
    "presence": dict[str, m.Presence] | None,
    "token": str | None,
    "config": m.Configuration,
    "messages": dict[str, list[m.Message]] | None,
    "updates": int | None,
    },
    total=False,
)

@dataclass
class EmptyParameters:
    pass

@dataclass
class ConfigParameters:
    subscribe: bool | None = None
    pushOptions: int | None = None
    settings: m.Configuration | None = None
    reset: bool | None = None
    syntheticDeviceId: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "subscribe": "subscribe",
        "pushOptions": "pushOptions",
        "settings": "settings",
        "reset": "reset",
        "syntheticDeviceId": "syntheticDeviceId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ConfigResponse = TypedDict(
    "ConfigResponse",
    {
    "hash": str | None,
    "user": m.UserSettings | None,
    "chats": dict[str, m.ChatSettings] | None,
    },
    total=False,
)

@dataclass
class AuthOneMeAuthConfirmParameters:
    token: str
    tokenType: str
    firstName: str
    lastName: str | None = None
    birthday: int | None = None
    photoId: int | None = None
    avatarType: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "token": "token",
        "tokenType": "tokenType",
        "firstName": "firstName",
        "lastName": "lastName",
        "birthday": "birthday",
        "photoId": "photoId",
        "avatarType": "avatarType",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthOneMeAuthConfirmResponse = TypedDict(
    "AuthOneMeAuthConfirmResponse",
    {
    "token": str,
    "tokenType": str,
    "profile": m.Profile | None,
    },
    total=False,
)

AvatarPresetAvatarResponse = TypedDict(
    "AvatarPresetAvatarResponse",
    {
    "presetAvatars": list[m.PresetAvatarsInfo] | None,
    },
    total=False,
)

@dataclass
class AssetsGetAssetCommonParameters:
    assetType: str | None = None
    sectionId: str | None = None
    _from: int | None = None
    count: int | None = None
    query: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "assetType": "assetType",
        "sectionId": "sectionId",
        "_from": "from",
        "count": "count",
        "query": "query",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AssetsGetAssetStickerSetResponse = TypedDict(
    "AssetsGetAssetStickerSetResponse",
    {
    "stickerSets": list[int],
    "marker": int,
    },
    total=False,
)

@dataclass
class AssetsUpdateParameters:
    type: str | None = None
    sync: int | None = None
    chatId: int | None = None
    userId: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "type": "type",
        "sync": "sync",
        "chatId": "chatId",
        "userId": "userId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AssetsUpdateResponse = TypedDict(
    "AssetsUpdateResponse",
    {
    "sync": int,
    "sections": list[m.AssetsSection] | None,
    "stickersUpdates": dict[str, int] | None,
    "stickerSetsUpdates": dict[str, int] | None,
    "stickersOrder": list[str] | None,
    "animojiUpdates": dict[str, int] | None,
    "animojiSetUpdates": dict[str, int] | None,
    },
    total=False,
)

@dataclass
class AssetsGetByIdsCommonParameters:
    type: str
    ids: list[int] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "type": "type",
        "ids": "ids",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AssetsGetByIdsAnimojiSetResponse = TypedDict(
    "AssetsGetByIdsAnimojiSetResponse",
    {
    "animojiSets": list[m.AnimojiSet],
    },
    total=False,
)

@dataclass
class AssetsAddParameters:
    type: str
    id: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "type": "type",
        "id": "id",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AssetsAddResponse = TypedDict(
    "AssetsAddResponse",
    {
    "success": bool | None,
    "updateTime": int | None,
    },
    total=False,
)

@dataclass
class ContactContactInfoParameters:
    contactType: str
    contactIds: list[int] = field(default_factory=list)
    chatId: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "contactIds": "contactIds",
        "contactType": "contactType",
        "chatId": "chatId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ContactContactInfoResponse = TypedDict(
    "ContactContactInfoResponse",
    {
    "contacts": list[m.Contact],
    },
    total=False,
)

@dataclass
class ContactContactUpdateParameters:
    contactId: int
    action: str
    firstName: str | None = None
    lastName: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "contactId": "contactId",
        "action": "action",
        "firstName": "firstName",
        "lastName": "lastName",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ContactContactUpdateResponse = TypedDict(
    "ContactContactUpdateResponse",
    {
    "contact": m.Contact | None,
    },
    total=False,
)

@dataclass
class ContactPresenceParameters:
    contactIds: list[int] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "contactIds": "contactIds",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ContactPresenceResponse = TypedDict(
    "ContactPresenceResponse",
    {
    "presence": dict[str, m.Presence],
    },
    total=False,
)

@dataclass
class ContactContactListParameters:
    status: str
    _from: int | None = None
    count: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "status": "status",
        "_from": "from",
        "count": "count",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ContactContactListResponse = TypedDict(
    "ContactContactListResponse",
    {
    "contacts": list[m.Contact],
    },
    total=False,
)

@dataclass
class SearchContactParameters:
    query: str
    count: int
    _from: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "query": "query",
        "count": "count",
        "_from": "from",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

SearchContactResponse = TypedDict(
    "SearchContactResponse",
    {
    "result": list[m.ContactResult] | None,
    "total": int,
    "ucpQId": str | None,
    },
    total=False,
)

@dataclass
class ContactContactPhotosParameters:
    contactId: int
    count: int | None = None
    _from: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "contactId": "contactId",
        "count": "count",
        "_from": "from",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ContactContactPhotosResponse = TypedDict(
    "ContactContactPhotosResponse",
    {
    "ids": list[int] | None,
    "urls": list[str],
    "total": int,
    },
    total=False,
)

@dataclass
class ContactContactCreateParameters:
    phone: str
    firstName: str | None = None
    lastName: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "phone": "phone",
        "firstName": "firstName",
        "lastName": "lastName",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ContactContactCreateResponse = TypedDict(
    "ContactContactCreateResponse",
    {
    "contact": m.Contact | None,
    "new": bool | None,
    },
    total=False,
)

@dataclass
class ContactRemoveContactPhotoParameters:
    photoId: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "photoId": "photoId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ContactRemoveContactPhotoResponse = TypedDict(
    "ContactRemoveContactPhotoResponse",
    {
    "profile": m.Profile | None,
    },
    total=False,
)

@dataclass
class ContactContactByPhoneParameters:
    phone: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "phone": "phone",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ContactContactByPhoneResponse = TypedDict(
    "ContactContactByPhoneResponse",
    {
    "contact": m.Contact | None,
    },
    total=False,
)

@dataclass
class ChatChatInfoParameters:
    chatIds: list[int] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatIds": "chatIds",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatChatInfoResponse = TypedDict(
    "ChatChatInfoResponse",
    {
    "chats": list[m.Chat],
    },
    total=False,
)

@dataclass
class ChatChatHistoryParameters:
    chatId: int
    _from: int
    backward: int
    backwardTime: int
    forward: int
    forwardTime: int
    postId: int | None = None
    itemType: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "postId": "postId",
        "_from": "from",
        "backward": "backward",
        "backwardTime": "backwardTime",
        "forward": "forward",
        "forwardTime": "forwardTime",
        "itemType": "itemType",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatChatHistoryResponse = TypedDict(
    "ChatChatHistoryResponse",
    {
    "messages": list[m.Message],
    },
    total=False,
)

@dataclass
class ChatChatMarkParameters:
    chatId: int
    mark: int | None = None
    messageId: int | None = None
    type: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "mark": "mark",
        "messageId": "messageId",
        "type": "type",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatChatMarkResponse = TypedDict(
    "ChatChatMarkResponse",
    {
    "mark": int | None,
    "unread": int | None,
    },
    total=False,
)

@dataclass
class ChatChatMediaParameters:
    chatId: int
    messageId: int | None = None
    attachTypes: list[str] | None = None
    fileAttachmentTypes: list[str] | None = None
    forward: int | None = None
    backward: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "messageId": "messageId",
        "attachTypes": "attachTypes",
        "fileAttachmentTypes": "fileAttachmentTypes",
        "forward": "forward",
        "backward": "backward",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatChatMediaResponse = TypedDict(
    "ChatChatMediaResponse",
    {
    "messages": list[m.Message],
    "pos": int | None,
    "total": int,
    "forward": int | None,
    "backward": int | None,
    },
    total=False,
)

@dataclass
class ChatChatDeleteParameters:
    chatId: int
    lastEventTime: int | None = None
    forAll: bool | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "lastEventTime": "lastEventTime",
        "forAll": "forAll",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

@dataclass
class ChatChatListParameters:
    marker: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "marker": "marker",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatChatListResponse = TypedDict(
    "ChatChatListResponse",
    {
    "chats": list[m.Chat],
    "marker": int,
    },
    total=False,
)

@dataclass
class ChatChatClearParameters:
    chatId: int
    lastEventTime: int
    forAll: bool | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "lastEventTime": "lastEventTime",
        "forAll": "forAll",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

@dataclass
class ChatChatUpdateParameters:
    chatId: int | None = None
    pinMessageId: int | None = None
    notifyPin: bool | None = None
    access: str | None = None
    theme: str | None = None
    description: str | None = None
    link: str | None = None
    photoToken: str | None = None
    revokePrivateLink: bool | None = None
    removeLink: bool | None = None
    changeOwnerId: int | None = None
    options: dict[str, bool] | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "pinMessageId": "pinMessageId",
        "notifyPin": "notifyPin",
        "access": "access",
        "theme": "theme",
        "description": "description",
        "link": "link",
        "photoToken": "photoToken",
        "revokePrivateLink": "revokePrivateLink",
        "removeLink": "removeLink",
        "changeOwnerId": "changeOwnerId",
        "options": "options",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatChatUpdateResponse = TypedDict(
    "ChatChatUpdateResponse",
    {
    "chat": m.Chat | None,
    },
    total=False,
)

@dataclass
class ChatCheckLinkParameters:
    link: str
    linkType: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "link": "link",
        "linkType": "linkType",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatCheckLinkResponse = TypedDict(
    "ChatCheckLinkResponse",
    {
    "message": str | None,
    },
    total=False,
)

@dataclass
class ChatJoinParameters:
    link: str | None = None
    chatAccessToken: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "link": "link",
        "chatAccessToken": "chatAccessToken",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatJoinResponse = TypedDict(
    "ChatJoinResponse",
    {
    "chat": m.Chat | None,
    },
    total=False,
)

@dataclass
class ChatChatLeaveParameters:
    chatId: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatChatLeaveResponse = TypedDict(
    "ChatChatLeaveResponse",
    {
    "message": m.Message | None,
    },
    total=False,
)

@dataclass
class ChatChatMembersParameters:
    chatId: int
    type: str | None = None
    marker: int | None = None
    count: int | None = None
    query: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "type": "type",
        "marker": "marker",
        "count": "count",
        "query": "query",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatChatMembersResponse = TypedDict(
    "ChatChatMembersResponse",
    {
    "members": list[m.ChatMember] | None,
    "marker": int | None,
    },
    total=False,
)

@dataclass
class SearchPublicParameters:
    query: str
    count: int
    marker: int
    type: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "query": "query",
        "count": "count",
        "marker": "marker",
        "type": "type",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

SearchPublicResponse = TypedDict(
    "SearchPublicResponse",
    {
    "result": list[m.PublicResult] | None,
    "total": int,
    "marker": int | None,
    "ucpQId": str | None,
    },
    total=False,
)

@dataclass
class ChatPersonalConfigParameters:
    chatId: int | None = None
    hideNonContactBar: bool | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "hideNonContactBar": "hideNonContactBar",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatPersonalConfigResponse = TypedDict(
    "ChatPersonalConfigResponse",
    {
    "chat": m.Chat | None,
    },
    total=False,
)

@dataclass
class LiveStreamLiveStreamInfoParameters:
    chatIds: list[int] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatIds": "chatIds",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

LiveStreamLiveStreamInfoResponse = TypedDict(
    "LiveStreamLiveStreamInfoResponse",
    {
    "liveStreams": list[m.LiveStream] | None,
    },
    total=False,
)

@dataclass
class MessagingSendParameters:
    message: m.OutgoingMessage
    chatId: int | None = None
    postId: int | None = None
    userId: int | None = None
    notify: bool | None = None
    lastKnownDraftTime: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "postId": "postId",
        "userId": "userId",
        "notify": "notify",
        "message": "message",
        "lastKnownDraftTime": "lastKnownDraftTime",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

MessagingSendResponse = TypedDict(
    "MessagingSendResponse",
    {
    "chatId": int,
    "postId": int | None,
    "message": m.Message,
    "chat": m.Chat | None,
    "unread": int | None,
    "mark": int | None,
    },
    total=False,
)

@dataclass
class TypingSendParameters:
    chatId: int
    type: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "type": "type",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

@dataclass
class MessagingDeleteParameters:
    chatId: int
    postId: int | None = None
    messageIds: list[int] = field(default_factory=list)
    complaint: str | None = None
    forMe: bool | None = None
    itemType: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "postId": "postId",
        "messageIds": "messageIds",
        "complaint": "complaint",
        "forMe": "forMe",
        "itemType": "itemType",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

MessagingDeleteResponse = TypedDict(
    "MessagingDeleteResponse",
    {
    "chatId": int,
    "postId": int | None,
    "messageIds": list[int],
    "failedMessageIds": list[int] | None,
    },
    total=False,
)

@dataclass
class MessagingEditParameters:
    chatId: int
    messageId: int
    postId: int | None = None
    text: str | None = None
    elements: list[m.MessageElement] | None = None
    attachments: list[dict] | None = None
    delayedAttributes: m.DelayedTamAttributes | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "postId": "postId",
        "messageId": "messageId",
        "text": "text",
        "elements": "elements",
        "attachments": "attachments",
        "delayedAttributes": "delayedAttributes",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

MessagingEditResponse = TypedDict(
    "MessagingEditResponse",
    {
    "message": m.Message,
    },
    total=False,
)

@dataclass
class SearchChatParameters:
    query: str
    count: int
    marker: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "query": "query",
        "count": "count",
        "marker": "marker",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

SearchChatResponse = TypedDict(
    "SearchChatResponse",
    {
    "result": list[m.ChatResult],
    "total": int | None,
    "counters": dict[str, int] | None,
    "marker": str | None,
    "ucpQId": str | None,
    },
    total=False,
)

@dataclass
class CallsVideoChatEditParameters:
    conversationId: str
    muteVideo: int | None = None
    muteAudio: int | None = None
    muteScreenSharing: int | None = None
    onlyAdminCanRecord: bool | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "conversationId": "conversationId",
        "muteVideo": "muteVideo",
        "muteAudio": "muteAudio",
        "muteScreenSharing": "muteScreenSharing",
        "onlyAdminCanRecord": "onlyAdminCanRecord",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

@dataclass
class MessagingMsgGetParameters:
    chatId: int
    messageIds: list[int] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "messageIds": "messageIds",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

MessagingMsgGetResponse = TypedDict(
    "MessagingMsgGetResponse",
    {
    "messages": list[m.Message],
    },
    total=False,
)

@dataclass
class SearchMessageTouchParameters:
    chatId: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

@dataclass
class SearchMessageParameters:
    chatId: int
    query: str
    count: int
    marker: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "query": "query",
        "count": "count",
        "marker": "marker",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

SearchMessageResponse = TypedDict(
    "SearchMessageResponse",
    {
    "result": list[m.MessageResult],
    "marker": int | None,
    "total": int | None,
    "ucpQId": str | None,
    },
    total=False,
)

@dataclass
class ChatMsgGetStatParameters:
    chatId: int
    messageIds: list[int] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "messageIds": "messageIds",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatMsgGetStatResponse = TypedDict(
    "ChatMsgGetStatResponse",
    {
    "stats": dict[str, m.MessageStats] | None,
    },
    total=False,
)

@dataclass
class ChatChatSubscribeParameters:
    chatId: int
    subscribe: bool
    postId: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "postId": "postId",
        "subscribe": "subscribe",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

@dataclass
class CallsVideoChatStartParameters:
    conversationId: str
    userId: int | None = None
    chatId: int | None = None
    type: str | None = None
    withJoinLink: bool | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "userId": "userId",
        "chatId": "chatId",
        "conversationId": "conversationId",
        "type": "type",
        "withJoinLink": "withJoinLink",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

CallsVideoChatStartResponse = TypedDict(
    "CallsVideoChatStartResponse",
    {
    "conversationId": str | None,
    "peerId": int | None,
    "joinLink": str | None,
    },
    total=False,
)

@dataclass
class ChatChatMembersUpdateParameters:
    chatId: int
    operation: str
    postId: int | None = None
    messageId: int | None = None
    userIds: list[int] = field(default_factory=list)
    type: str | None = None
    showHistory: bool | None = None
    permissions: int | None = None
    cleanMsgPeriod: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "postId": "postId",
        "messageId": "messageId",
        "operation": "operation",
        "userIds": "userIds",
        "type": "type",
        "showHistory": "showHistory",
        "permissions": "permissions",
        "cleanMsgPeriod": "cleanMsgPeriod",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatChatMembersUpdateResponse = TypedDict(
    "ChatChatMembersUpdateResponse",
    {
    "chat": m.Chat | None,
    "deletedMessageIds": list[int] | None,
    },
    total=False,
)

@dataclass
class CallsVideoChatStartActiveParameters:
    conversationId: str
    isVideo: bool
    internalParams: str
    calleeIds: list[int] | None = None
    chatId: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "conversationId": "conversationId",
        "calleeIds": "calleeIds",
        "chatId": "chatId",
        "isVideo": "isVideo",
        "internalParams": "internalParams",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

CallsVideoChatStartActiveResponse = TypedDict(
    "CallsVideoChatStartActiveResponse",
    {
    "rejectedParticipants": list[m.RejectedParticipant] | None,
    "conversationId": str | None,
    "internalCallerParams": str | None,
    "joinLink": str | None,
    },
    total=False,
)

@dataclass
class CallsVideoChatHistoryParameters:
    marker: int
    count: int
    forward: bool | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "marker": "marker",
        "count": "count",
        "forward": "forward",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

CallsVideoChatHistoryResponse = TypedDict(
    "CallsVideoChatHistoryResponse",
    {
    "history": list[m.CallHistory],
    "forwardMarker": int | None,
    "backwardMarker": int | None,
    "hasMore": bool | None,
    },
    total=False,
)

@dataclass
class AttachmentsPhotoUploadParameters:
    count: int
    profile: bool | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "count": "count",
        "profile": "profile",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AttachmentsPhotoUploadResponse = TypedDict(
    "AttachmentsPhotoUploadResponse",
    {
    "url": str,
    },
    total=False,
)

@dataclass
class AttachmentsVideoUploadParameters:
    count: int
    type: int | None = None
    uploaderType: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "count": "count",
        "type": "type",
        "uploaderType": "uploaderType",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AttachmentsVideoUploadResponse = TypedDict(
    "AttachmentsVideoUploadResponse",
    {
    "info": list[m.AttachmentsVideoUploadVideoUploadInfo],
    "uploaderType": int | None,
    },
    total=False,
)

@dataclass
class AttachmentsVideoPlayParameters:
    videoId: int
    chatId: int | None = None
    messageId: int | None = None
    token: str | None = None
    itemType: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "videoId": "videoId",
        "chatId": "chatId",
        "messageId": "messageId",
        "token": "token",
        "itemType": "itemType",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AttachmentsVideoPlayResponse = TypedDict(
    "AttachmentsVideoPlayResponse",
    {
    "MP4_144": str | None,
    "MP4_240": str | None,
    "MP4_360": str | None,
    "MP4_480": str | None,
    "MP4_720": str | None,
    "MP4_1080": str | None,
    "DASH": str | None,
    "O_DASH": str | None,
    "HLS": str | None,
    "O_HLS": str | None,
    "EXTERNAL": str | None,
    "live": bool | None,
    "startTime": int | None,
    },
    total=False,
)

@dataclass
class CallsVideoChatCreateJoinLinkParameters:
    conversationId: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "conversationId": "conversationId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

CallsVideoChatCreateJoinLinkResponse = TypedDict(
    "CallsVideoChatCreateJoinLinkResponse",
    {
    "joinLink": str,
    },
    total=False,
)

@dataclass
class ChatChatPinSetVisibilityParameters:
    chatId: int
    show: bool

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "show": "show",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatChatPinSetVisibilityResponse = TypedDict(
    "ChatChatPinSetVisibilityResponse",
    {
    "chat": m.Chat | None,
    },
    total=False,
)

@dataclass
class AttachmentsFileUploadParameters:
    count: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "count": "count",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AttachmentsFileUploadResponse = TypedDict(
    "AttachmentsFileUploadResponse",
    {
    "info": list[m.AttachmentsFileUploadFileUploadInfo],
    },
    total=False,
)

@dataclass
class AttachmentsFileDownloadParameters:
    fileId: int
    chatId: int
    messageId: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "fileId": "fileId",
        "chatId": "chatId",
        "messageId": "messageId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AttachmentsFileDownloadResponse = TypedDict(
    "AttachmentsFileDownloadResponse",
    {
    "url": str,
    "unsafe": bool | None,
    },
    total=False,
)

@dataclass
class LinkInfoParameters:
    link: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "link": "link",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

LinkInfoResponse = TypedDict(
    "LinkInfoResponse",
    {
    "chat": m.Chat | None,
    "user": m.ContactResult | None,
    "message": m.Message | None,
    "startPayload": str | None,
    "stickerSet": m.StickerSet | None,
    "videoConference": m.VideoConference | None,
    },
    total=False,
)

SessionsInfoResponse = TypedDict(
    "SessionsInfoResponse",
    {
    "sessions": list[m.Session],
    },
    total=False,
)

SessionsCloseResponse = TypedDict(
    "SessionsCloseResponse",
    {
    "token": str,
    },
    total=False,
)

@dataclass
class AuthOneMeLoginRestorePasswordParameters:
    password: str
    trackId: str
    hint: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "password": "password",
        "trackId": "trackId",
        "hint": "hint",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthOneMeLoginRestorePasswordResponse = TypedDict(
    "AuthOneMeLoginRestorePasswordResponse",
    {
    "tokenAttrs": dict[str, m.AuthOneMeTokenAttributes],
    "profile": m.Profile | None,
    },
    total=False,
)

@dataclass
class AuthOneMeDetails2FAParameters:
    trackId: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "trackId": "trackId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthOneMeDetails2FAResponse = TypedDict(
    "AuthOneMeDetails2FAResponse",
    {
    "password": m.AuthOneMeDetails2FAResponsePassword,
    },
    total=False,
)

@dataclass
class ExternalCallbackParameters:
    url: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "url": "url",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ExternalCallbackResponse = TypedDict(
    "ExternalCallbackResponse",
    {
    "botId": int | None,
    "startParam": str | None,
    },
    total=False,
)

@dataclass
class WebAppWebAppRequestPhoneHashParameters:
    botId: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "botId": "botId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

WebAppWebAppRequestPhoneHashResponse = TypedDict(
    "WebAppWebAppRequestPhoneHashResponse",
    {
    "phone": str,
    "hash": str,
    },
    total=False,
)

@dataclass
class AuthOneMeValidatePasswordParameters:
    trackId: str
    password: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "trackId": "trackId",
        "password": "password",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

@dataclass
class AuthOneMeValidateHintParameters:
    trackId: str
    hint: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "trackId": "trackId",
        "hint": "hint",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

@dataclass
class AuthOneMeVerifyEmailParameters:
    trackId: str
    email: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "email": "email",
        "trackId": "trackId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthOneMeVerifyEmailResponse = TypedDict(
    "AuthOneMeVerifyEmailResponse",
    {
    "codeLength": int,
    "blockingDuration": int,
    "trackId": str,
    },
    total=False,
)

@dataclass
class AuthOneMeCheckEmailParameters:
    verifyCode: str
    trackId: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "verifyCode": "verifyCode",
        "trackId": "trackId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthOneMeCheckEmailResponse = TypedDict(
    "AuthOneMeCheckEmailResponse",
    {
    "email": str,
    "trackId": str,
    },
    total=False,
)

@dataclass
class AuthOneMeSet2FAParameters:
    trackId: str
    hint: str | None = None
    password: str | None = None
    remove2fa: bool | None = None
    expectedCapabilities: list[int] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "hint": "hint",
        "password": "password",
        "remove2fa": "remove2fa",
        "expectedCapabilities": "expectedCapabilities",
        "trackId": "trackId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

@dataclass
class AuthOneMeCreateTrackParameters:
    type: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "type": "type",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthOneMeCreateTrackResponse = TypedDict(
    "AuthOneMeCreateTrackResponse",
    {
    "trackId": str,
    },
    total=False,
)

@dataclass
class AuthOneMeCheckPasswordParameters:
    password: str
    trackId: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "password": "password",
        "trackId": "trackId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthOneMeCheckPasswordResponse = TypedDict(
    "AuthOneMeCheckPasswordResponse",
    {
    "trackId": str,
    },
    total=False,
)

@dataclass
class AuthOneMeLoginCheckPasswordParameters:
    password: str
    trackId: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "password": "password",
        "trackId": "trackId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthOneMeLoginCheckPasswordResponse = TypedDict(
    "AuthOneMeLoginCheckPasswordResponse",
    {
    "tokenAttrs": dict[str, m.AuthOneMeTokenAttributes],
    "profile": m.Profile | None,
    },
    total=False,
)

@dataclass
class AuthOneMeLoginProfileDeleteParameters:
    trackId: str
    delete: bool

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "trackId": "trackId",
        "delete": "delete",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthOneMeLoginProfileDeleteResponse = TypedDict(
    "AuthOneMeLoginProfileDeleteResponse",
    {
    "timestamp": int | None,
    },
    total=False,
)

@dataclass
class MessagingSendCallbackParameters:
    callbackId: str
    type: str | None = None
    payload: str | None = None
    timestamp: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "callbackId": "callbackId",
        "type": "type",
        "payload": "payload",
        "timestamp": "timestamp",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

MessagingSendCallbackResponse = TypedDict(
    "MessagingSendCallbackResponse",
    {
    "success": bool,
    "message": m.Message | None,
    "chat": m.Chat | None,
    "chatAccessToken": str | None,
    },
    total=False,
)

@dataclass
class BotSuspendParameters:
    botId: int
    value: bool

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "botId": "botId",
        "value": "value",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

EventsNotificationCallAcceptedDataResponse = TypedDict(
    "EventsNotificationCallAcceptedDataResponse",
    {
    "conversationId": str,
    },
    total=False,
)

EventsNotificationCallFinishDataResponse = TypedDict(
    "EventsNotificationCallFinishDataResponse",
    {
    "conversationId": str,
    },
    total=False,
)

EventsNotificationMessageDataPayload = TypedDict(
    "EventsNotificationMessageDataPayload",
    {
    "chatId": int,
    "postId": int | None,
    "message": m.Message,
    "prevMessageId": int | None,
    "unread": int | None,
    "mark": int | None,
    },
    total=False,
)

EventsTypingDataPayload = TypedDict(
    "EventsTypingDataPayload",
    {
    "chatId": int,
    "userId": int,
    "type": str | None,
    },
    total=False,
)

EventsNotificationMarkDataResponse = TypedDict(
    "EventsNotificationMarkDataResponse",
    {
    "chatId": int,
    "userId": int,
    "mark": int,
    "unread": int | None,
    "setAsUnread": bool | None,
    },
    total=False,
)

EventsNotificationContactDataResponse = TypedDict(
    "EventsNotificationContactDataResponse",
    {
    "contact": m.Contact,
    },
    total=False,
)

EventsPresenceNotificationMessageDataPayload = TypedDict(
    "EventsPresenceNotificationMessageDataPayload",
    {
    "userId": int,
    "presence": m.Presence,
    "time": int | None,
    },
    total=False,
)

EventsConfigChangeDataPayload = TypedDict(
    "EventsConfigChangeDataPayload",
    {
    "config": m.Configuration,
    },
    total=False,
)

EventsNotificationChatDataResponse = TypedDict(
    "EventsNotificationChatDataResponse",
    {
    "chat": m.Chat,
    },
    total=False,
)

EventsNotificationAttachDataResponse = TypedDict(
    "EventsNotificationAttachDataResponse",
    {
    "videoId": int | None,
    "audioId": int | None,
    "fileId": int | None,
    "error": str | None,
    },
    total=False,
)

IncomingCall = TypedDict(
    "IncomingCall",
    {
    "conversationId": str,
    "callerId": int,
    "chatId": int | None,
    "sdpOffer": str | None,
    "type": str | None,
    "isContact": bool | None,
    "country": str | None,
    },
    total=False,
)

EventsMessagesDeleteRangeDataResponse = TypedDict(
    "EventsMessagesDeleteRangeDataResponse",
    {
    "chat": m.Chat,
    "startTime": int,
    "endTime": int,
    },
    total=False,
)

EventsMessagesDeleteDataResponse = TypedDict(
    "EventsMessagesDeleteDataResponse",
    {
    "chat": m.Chat,
    "messageIds": list[int],
    "ttl": bool | None,
    },
    total=False,
)

@dataclass
class BotBotInfoParameters:
    botId: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "botId": "botId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

BotBotInfoResponse = TypedDict(
    "BotBotInfoResponse",
    {
    "commands": list[m.BotBotCommand],
    "contact": m.Contact,
    "startMessage": m.BotStartMessage | None,
    },
    total=False,
)

AssetsUpdateContent = TypedDict(
    "AssetsUpdateContent",
    {
    "type": str,
    "id": int | None,
    "ids": list[int] | None,
    "updateType": str,
    "position": int | None,
    "sync": int | None,
    "recentsList": list[m.AssetsRecentItem] | None,
    "recentEmojiList": list[m.AssetsRecentEmojiItem] | None,
    },
    total=False,
)

EventsNotificationMessageDelayedDataPayload = TypedDict(
    "EventsNotificationMessageDelayedDataPayload",
    {
    "chatId": int,
    "userId": int,
    "updateTypeId": int,
    "message": m.Message | None,
    "messageIds": list[int] | None,
    "lastDelayedUpdateTime": int,
    },
    total=False,
)

EventsReactionsChangedDataResponse = TypedDict(
    "EventsReactionsChangedDataResponse",
    {
    "chatId": int,
    "postId": int | None,
    "messageId": int,
    "totalCount": int,
    "counters": list[m.MessageReactionCounter] | None,
    },
    total=False,
)

EventsYouReactedDataResponse = TypedDict(
    "EventsYouReactedDataResponse",
    {
    "chatId": int,
    "postId": int | None,
    "messageId": int,
    "reactionInfo": m.MessageReactionInfo | None,
    },
    total=False,
)

@dataclass
class CallsCallsTokenParameters:
    userId: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "userId": "userId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

CallsCallsTokenResponse = TypedDict(
    "CallsCallsTokenResponse",
    {
    "token": str,
    },
    total=False,
)

EventsNotificationProfiletDataResponse = TypedDict(
    "EventsNotificationProfiletDataResponse",
    {
    "profile": m.Profile,
    },
    total=False,
)

@dataclass
class WebAppWebAppInitDataParameters:
    botId: int
    startParam: str
    chatId: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "botId": "botId",
        "chatId": "chatId",
        "startParam": "startParam",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

WebAppWebAppInitDataResponse = TypedDict(
    "WebAppWebAppInitDataResponse",
    {
    "url": str | None,
    "query_id": str | None,
    },
    total=False,
)

@dataclass
class ComplainParameters:
    typeId: int | None = None
    reasonId: int | None = None
    ids: list[int] = field(default_factory=list)
    parentId: int | None = None
    postId: int | None = None
    type: str | None = None
    reason: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "typeId": "typeId",
        "reasonId": "reasonId",
        "ids": "ids",
        "parentId": "parentId",
        "postId": "postId",
        "type": "type",
        "reason": "reason",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

@dataclass
class ComplainReasonsGetParameters:
    complainSync: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "complainSync": "complainSync",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ComplainReasonsGetResponse = TypedDict(
    "ComplainReasonsGetResponse",
    {
    "complainSync": int | None,
    "complains": list[m.ComplainReasonsGetComplainsReason] | None,
    },
    total=False,
)

@dataclass
class CallsVideoChatJoinParameters:
    joinLink: str
    isVideo: bool
    internalParams: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "joinLink": "joinLink",
        "isVideo": "isVideo",
        "internalParams": "internalParams",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

CallsVideoChatJoinResponse = TypedDict(
    "CallsVideoChatJoinResponse",
    {
    "conversationId": str,
    "internalParams": str,
    },
    total=False,
)

@dataclass
class ReactionsMsgReactParameters:
    chatId: int
    messageId: int
    reaction: m.MessageReaction
    postId: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "postId": "postId",
        "messageId": "messageId",
        "reaction": "reaction",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ReactionsMsgReactResponse = TypedDict(
    "ReactionsMsgReactResponse",
    {
    "reactionInfo": m.MessageReactionInfo,
    },
    total=False,
)

@dataclass
class ReactionsCancelReactionParameters:
    chatId: int
    messageId: int
    postId: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "postId": "postId",
        "messageId": "messageId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ReactionsCancelReactionResponse = TypedDict(
    "ReactionsCancelReactionResponse",
    {
    "reactionInfo": m.MessageReactionInfo,
    },
    total=False,
)

@dataclass
class ReactionsGetReactionsParameters:
    chatId: int
    postId: int | None = None
    messageIds: list[int] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "postId": "postId",
        "messageIds": "messageIds",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ReactionsGetReactionsResponse = TypedDict(
    "ReactionsGetReactionsResponse",
    {
    "messagesReactions": dict[str, m.MessageReactionInfo],
    },
    total=False,
)

@dataclass
class ReactionsGetDetailedReactionsParameters:
    chatId: int
    messageId: int
    postId: int | None = None
    reaction: str | None = None
    marker: int | None = None
    count: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "postId": "postId",
        "messageId": "messageId",
        "reaction": "reaction",
        "marker": "marker",
        "count": "count",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ReactionsGetDetailedReactionsResponse = TypedDict(
    "ReactionsGetDetailedReactionsResponse",
    {
    "reactions": list[m.MessageReactionEntry] | None,
    "reactionInfo": m.MessageReactionInfo | None,
    "yourReaction": m.MessageReactionEntry | None,
    "marker": int | None,
    },
    total=False,
)

@dataclass
class ChatCommonChatListParameters:
    userIds: list[int] = field(default_factory=list)
    count: int | None = None
    marker: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "userIds": "userIds",
        "count": "count",
        "marker": "marker",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ChatCommonChatListResponse = TypedDict(
    "ChatCommonChatListResponse",
    {
    "commonChats": list[m.Chat],
    "hasMore": bool | None,
    "marker": int | None,
    },
    total=False,
)

@dataclass
class ProfileDeleteParameters:
    delete: bool
    type: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "delete": "delete",
        "type": "type",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ProfileDeleteResponse = TypedDict(
    "ProfileDeleteResponse",
    {
    "timestamp": int | None,
    },
    total=False,
)

GetDeleteTamUserTimeResponse = TypedDict(
    "GetDeleteTamUserTimeResponse",
    {
    "timestamp": int,
    },
    total=False,
)

@dataclass
class TranscribeMediaParameters:
    chatId: int
    messageId: int
    mediaId: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "messageId": "messageId",
        "mediaId": "mediaId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

TranscribeMediaResponse = TypedDict(
    "TranscribeMediaResponse",
    {
    "transcription": str | None,
    "transcriptionStatus": int,
    },
    total=False,
)

@dataclass
class OrgInfoParameters:
    organizationIds: list[int] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "organizationIds": "organizationIds",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

OrgInfoResponse = TypedDict(
    "OrgInfoResponse",
    {
    "organizations": list[m.Organization],
    },
    total=False,
)

@dataclass
class ReactionsSetChatReactionsParameters:
    chatId: int
    value: bool | None = None
    count: int | None = None
    included: bool | None = None
    reactionIds: list[str] | None = None
    reset: bool | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "value": "value",
        "count": "count",
        "included": "included",
        "reactionIds": "reactionIds",
        "reset": "reset",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ReactionsSetChatReactionsResponse = TypedDict(
    "ReactionsSetChatReactionsResponse",
    {
    "chatReactionsSettings": m.ChatReactionsSettings | None,
    },
    total=False,
)

@dataclass
class ReactionsGetChatReactionsParameters:
    chatIds: list[int] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatIds": "chatIds",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

ReactionsGetChatReactionsResponse = TypedDict(
    "ReactionsGetChatReactionsResponse",
    {
    "chatReactionsSettings": list[m.ChatReactionsSettings] | None,
    },
    total=False,
)

@dataclass
class AssetsRemoveParameters:
    type: str
    ids: list[int] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "type": "type",
        "ids": "ids",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AssetsRemoveResponse = TypedDict(
    "AssetsRemoveResponse",
    {
    "success": bool | None,
    "updateTime": int | None,
    },
    total=False,
)

@dataclass
class AssetsListModifyParameters:
    type: str
    ids: list[int] = field(default_factory=list)
    updateTime: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "type": "type",
        "ids": "ids",
        "updateTime": "updateTime",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AssetsListModifyResponse = TypedDict(
    "AssetsListModifyResponse",
    {
    "success": bool | None,
    "updateTime": int | None,
    },
    total=False,
)

@dataclass
class FoldersGetParameters:
    folderSync: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "folderSync": "folderSync",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

FoldersGetResponse = TypedDict(
    "FoldersGetResponse",
    {
    "folderSync": int,
    "folders": list[m.ChatFolder] | None,
    "foldersOrder": list[str] | None,
    "allFilterExcludeFolders": list[str] | None,
    },
    total=False,
)

@dataclass
class FoldersUpdateParameters:
    id: str
    title: str
    emoji: str | None = None
    include: list[int] | None = None
    favorites: list[int] | None = None
    filters: list[int] | None = None
    options: list[int] | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "id": "id",
        "title": "title",
        "emoji": "emoji",
        "include": "include",
        "favorites": "favorites",
        "filters": "filters",
        "options": "options",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

FoldersUpdateResponse = TypedDict(
    "FoldersUpdateResponse",
    {
    "folder": m.ChatFolder,
    "folderSync": int,
    "foldersOrder": list[str] | None,
    },
    total=False,
)

@dataclass
class FoldersReorderParameters:
    foldersOrder: list[str] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "foldersOrder": "foldersOrder",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

FoldersReorderResponse = TypedDict(
    "FoldersReorderResponse",
    {
    "folderSync": int,
    "foldersOrder": list[str],
    },
    total=False,
)

@dataclass
class FoldersDeleteParameters:
    folderIds: list[str] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "folderIds": "folderIds",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

FoldersDeleteResponse = TypedDict(
    "FoldersDeleteResponse",
    {
    "folderSync": int,
    "foldersOrder": list[str],
    },
    total=False,
)

EventsNotificationFoldersDataResponse = TypedDict(
    "EventsNotificationFoldersDataResponse",
    {
    "folderSync": int,
    "folders": list[m.ChatFolder] | None,
    "foldersOrder": list[str] | None,
    "allFilterExcludeFolders": list[str] | None,
    },
    total=False,
)

AuthOneMeCreateQrResponse = TypedDict(
    "AuthOneMeCreateQrResponse",
    {
    "trackId": str | None,
    "pollingInterval": int | None,
    "ttl": int | None,
    "qrLink": str | None,
    },
    total=False,
)

@dataclass
class AuthOneMeAuthCheckParameters:
    trackId: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "trackId": "trackId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthOneMeAuthCheckResponse = TypedDict(
    "AuthOneMeAuthCheckResponse",
    {
    "status": m.AuthOneMeAuthCheckResponseStatus | None,
    },
    total=False,
)

@dataclass
class AuthOneMeAuthByTrackParameters:
    trackId: str

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "trackId": "trackId",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AuthOneMeAuthByTrackResponse = TypedDict(
    "AuthOneMeAuthByTrackResponse",
    {
    "tokenAttrs": dict[str, m.AuthOneMeTokenAttributes] | None,
    "profile": m.Profile | None,
    "passwordChallenge": m.AuthOneMePasswordChallenge | None,
    },
    total=False,
)

EventsBannersDataPayload = TypedDict(
    "EventsBannersDataPayload",
    {
    "showTime": int,
    "banners": list[m.BannerProtoV1],
    "updateTime": int,
    },
    total=False,
)

EventsNotificationTranscriptionDataPayload = TypedDict(
    "EventsNotificationTranscriptionDataPayload",
    {
    "chatId": int,
    "messageId": int,
    "mediaId": int,
    "transcription": str | None,
    "transcriptionStatus": int,
    },
    total=False,
)

@dataclass
class SuggestChatSuggestParameters:
    chatId: int | None = None
    folderId: str | None = None
    userChatIds: list[int] | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "folderId": "folderId",
        "userChatIds": "userChatIds",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

SuggestChatSuggestResponse = TypedDict(
    "SuggestChatSuggestResponse",
    {
    "chats": list[m.Chat],
    },
    total=False,
)

@dataclass
class AttachmentsAudioPlayParameters:
    audioId: int
    chatId: int | None = None
    messageId: int | None = None
    token: str | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "audioId": "audioId",
        "chatId": "chatId",
        "messageId": "messageId",
        "token": "token",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

AttachmentsAudioPlayResponse = TypedDict(
    "AttachmentsAudioPlayResponse",
    {
    "opus": str | None,
    "m4a": str | None,
    "mp3": str | None,
    },
    total=False,
)

@dataclass
class BannersBannersGetParameters:
    bannersSync: int

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "bannersSync": "bannersSync",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

BannersBannersGetPayload = TypedDict(
    "BannersBannersGetPayload",
    {
    "showTime": int | None,
    "banners": list[m.Banner] | None,
    "updateTime": int,
    },
    total=False,
)

@dataclass
class PollsSendVoteParameters:
    chatId: int
    pollId: int
    messageId: int
    answersIds: list[int] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "pollId": "pollId",
        "messageId": "messageId",
        "answersIds": "answersIds",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

PollsSendVoteResponse = TypedDict(
    "PollsSendVoteResponse",
    {
    "state": m.PollState,
    },
    total=False,
)

@dataclass
class PollsVotersListByAnswerParameters:
    pollId: int
    chatId: int
    messageId: int
    answerId: int
    marker: int | None = None
    count: int | None = None

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "pollId": "pollId",
        "chatId": "chatId",
        "messageId": "messageId",
        "answerId": "answerId",
        "marker": "marker",
        "count": "count",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

PollsVotersListByAnswerResponse = TypedDict(
    "PollsVotersListByAnswerResponse",
    {
    "marker": int | None,
    "voters": list[m.PollAnswerStats] | None,
    "voteCount": int | None,
    },
    total=False,
)

@dataclass
class PollsGetPollUpdatesParameters:
    chatId: int
    polls: list[m.PollsGetPollUpdatesParametersChatPollUpdate] = field(default_factory=list)

    _FIELD_MAP: ClassVar[dict[str, str]] = {
        "chatId": "chatId",
        "polls": "polls",
    }

    def to_dict(self) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for py_attr, orig_key in self._FIELD_MAP.items():
            val = getattr(self, py_attr)
            if val is not None:
                if isinstance(val, list):
                    if val:
                        result[orig_key] = val
                elif isinstance(val, dict):
                    if val:
                        result[orig_key] = val
                else:
                    result[orig_key] = val
        return result

PollsGetPollUpdatesResponse = TypedDict(
    "PollsGetPollUpdatesResponse",
    {
    "polls": list[m.Poll],
    },
    total=False,
)

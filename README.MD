# mah

Исследование desktop клиента MAX

>[!warning]
> Это исследовательский проект для изучения принципа работы протокола мессенджера. Здесь не будет готовых библиотек, фреймворков или production-решений.
>
> Если вы будете применять наработки для изготовления ПО, то вы можете получить блокировку аккаунта за нарушение пользовательского соглашения https://legal.max.ru/ps
>
> 4.3: Пользователю при использовании Сервиса запрещается:
> 
> 4.3.7 использовать без специального на то разрешения Компании автоматизированные скрипты (программы, боты, краулеры) для сбора и/или извлечения информации в Сервисе и/или взаимодействия с Сервисом и его функциональностью, производить обратную разработку, декомпиляцию, обратное ассемблирование, изменение, дублирование, создание копий, создание производных работ, осуществлять распространение или предоставление иным лицам программного обеспечения или иной информации, доступной, полученной или извлеченной из Сервиса или любой его части (за исключением Контента, загруженного самим Пользователем);
>
> 4.3.10 осуществлять использование Сервиса иным способом, кроме как через интерфейс, предоставленный Компанией, блокировать, отключать или иным образом влиять на любые функции Сервиса, за исключением случаев, когда такие действия были прямо разрешены Пользователю в соответствии с отдельным соглашением с Компанией; 

## Что это

Реверс-инжиниринг протокола desktop-клиента MAX: дампер структур пакетов из `core.dll`, документация формата пакетов/транспорта. Полный пайплайн:

```
+-------------+   +---------------------------------+   +------------------------------------+
| core.dll    |-->| dumper (Rust / IDA / Binja)     |-->| packets.json / packets_rust.json   |
+-------------+   +---------------------------------+   +------------------------------------+
                         структуры полей                              c++ типы / схемы
```

## Структура репозитория

```
.
└── dev/                  дамперы, конфигурация, документация протокола
    ├── dumper_rust/      автономный дампер на Rust (~300 мс, $0 лицензий, RTTI/iced-x86)
    ├── dumper/           дампер для IDA Pro 7.1+ (hex-rays)
    ├── dumper_binja/     дампер для Binary Ninja 5.2+ (IL-based, точнее по типам полей)
    ├── config_extractor.py  извлечение config.json (api-версия, endpoint, user-agent)
    ├── packets.json       актуальный дамп (IDA)
    ├── packets_binja.json актуальный дамп (Binary ninja)
    ├── packets_rust.json  актуальный дамп (Rust)
    └── README.md         документация протокола и дампа
```

## Протокол (TL;DR)

TCP/TLS, фиксированный **10-байтный заголовок** (big-endian), тело сериализуется через **msgpack** с опциональным **lz4**-сжатием. Ответы сопоставляются с запросами по полю `seq`. Метаданные (api endpoint, версия RPC, user-agent, интервал PING) берутся из извлечённого `config.json`.

Подробности: формат пакета, диаграммы send/receive, референс транспорта — в **[dev/README.md](dev/README.md)**.

## Требования

| Компонент | Что нужно |
| --- | --- |
| Дампер (Rust) | Rust toolchain (`cargo build --release`), без внешних дизассемблеров |
| Дампер (IDA) | IDA Pro 7.1+ с декомпилятором hex-rays (free не подойдёт) |
| Дампер (Binja) | Binary Ninja 5.2+, версия Personal или выше |
| Дамп config | Python 3.8+ |

## See also:

- DEVEL для разработчиков-исследователей [dev/README.md](dev/README.md)
